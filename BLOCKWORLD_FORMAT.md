# BlockWorld座標ファイルフォーマット

## 概要
BlockWorldでは、座標情報ファイルを使用してゲーム起動時にオブジェクト配置情報を読み込みます。



### 新フォーマット（v2.0）
オブジェクトの種類とサイズを含む：
```
x,y,z,objectType,size
4.500000,0.500000,3.500000,Cube,1.00
0.000000,0.000000,0.000000,Plane,10.00
-2.500000,0.500000,-4.500000,Cube,0.50
```

## パラメータの説明

| パラメータ | 型 | 説明 | 例 |
|-----------|-----|------|-----|
| x | float | ワールド座標のX値 | 4.5 |
| y | float | ワールド座標のY値 | 0.5 |
| z | float | ワールド座標のZ値 | 3.5 |
| objectType | string | オブジェクトの種類 | Cube, Plane |
| size | float | オブジェクトのサイズ | 1.0, 10.0 |

## サポートされているオブジェクト型

### Cube
- **説明**: 立方体ブロック
- **デフォルトサイズ**: 1.0
- **推奨範囲**: 0.5～2.0
- **用途**: メインのビルディングブロック
- **例**: `2.5,0.5,1.5,Cube,1.0`

### Plane
- **説明**: 平面（地形や装飾に使用）
- **デフォルトサイズ**: 10.0
- **推奨範囲**: 5.0～20.0
- **用途**: 地面、壁などの装飾要素
- **例**: `0.0,0.0,0.0,Plane,10.0`

## 拡張予定

将来的には以下のオブジェクト型のサポートも計画しています：
- Sphere（球体）
- Cylinder（円柱）
- Door（ドア）
- Decoration（その他の装飾）

## 使用例

```
# 基本的なCubeブロック配置
4.500000,0.500000,3.500000,Cube,1.00
4.500000,0.500000,2.500000,Cube,1.00

# 小さなCubeブロック（サイズ0.5）
2.500000,1.500000,1.500000,Cube,0.50

# 大きな床Plane
0.000000,-5.000000,0.000000,Plane,20.00

# Planeを壁として使用
-10.000000,0.000000,0.000000,Plane,10.00
```

## ファイル読み込みの仕動作

### 旧フォーマット互換性
- 既存の旧フォーマット（x,y,zのみ）のファイルも読み込み可能です
- `readCoordinatesFromFile()`メソッドは旧フォーマット対応
- `readBlockDataFromFile()`メソッドは新フォーマット対応

### エラーハンドリング
- パース中にエラーが発生した行はスキップされ、警告が表示されます
- ファイル全体の読み込みが停止することはありません
- 不正なオブジェクト型は該当行をスキップします

## 実装コード例

```cpp
// 新フォーマットでファイルから読み込む
BlockWorld blockWorld(collisionMesh);
if (blockWorld.LoadCubeStateFromFile("coordinates_new.txt")) {
    std::cout << "ファイル読み込み成功\n";
} else {
    std::cerr << "ファイル読み込み失敗\n";
}

// ゲーム内でブロックを配置したら、セーブ
blockWorld.SaveToFile("coordinates_new.txt", "output");
```

## トラブルシューティング

### ファイルが見つからない
```
エラー: ファイルを開けません: coordinates_new.txt
```
- ファイルパスが正しいか確認してください
- ファイルが存在するか確認してください

### パースエラー
```
警告: 列数が不正です (行 5, 期待値: 5, 実際: 3)
```
- その行にコンマ区切りで5つの値があるか確認してください
- 余分な空白がないか確認してください

### 未対応のオブジェクト型
```
警告: 未対応のオブジェクト種類です: Sphere
```
- objectTypeが正しくスペルされているか確認してください
- 現在はCubeとPlaneのみサポートされています
